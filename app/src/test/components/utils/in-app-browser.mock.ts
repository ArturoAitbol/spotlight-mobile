import { InAppBrowserOptions } from '@awesome-cordova-plugins/in-app-browser';

export const IN_APP_BROWSER_MOCK = {
  create: (url: string, target?: string, options?: string | InAppBrowserOptions)=>{

  } 
};
