import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tekvizion.spotlight',
  appName: 'tekvizion-spotlight',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
